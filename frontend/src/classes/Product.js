export default class Product {
    constructor(sku, name, price, productType) {
        this.sku = sku;
        this.name = name;
        this.price = price;
        this.productType = productType;
    }
}